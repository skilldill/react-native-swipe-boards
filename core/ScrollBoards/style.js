import {StyleSheet} from "react-native";

export const scrollboardsStyle = StyleSheet.create({
    container: {
        flex: 1
    },

    board: {
        width: '100%'
    },

    boards: {
        flex: 1,
        flexDirection: 'row',
    }
})